import "..\\mac\\extra\\collections\\collections.mac";
var list = ArrayList();
//<
list.addItems(1, 2, 3);
var otherList = ArrayList().addItems(1, 2, 3, 4);
var result = list.containsAll(otherList);
println(result);           // false
result = otherList.containsAll(list);
println(result);           // true
//>