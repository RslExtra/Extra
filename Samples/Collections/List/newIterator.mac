import "..\\mac\\extra\\collections\\collections.mac";
var list = ArrayList();
//<
list.addItems(1, 2, 3, 4);
var itr = list.newIterator();

while (itr.hasNext())
    println(itr.next());   // выведет все числа на экран
end;
//>