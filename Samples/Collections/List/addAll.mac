import "..\\mac\\extra\\collections\\collections.mac";
var list = ArrayList();
//<
var arr = TArray();
arr[0] = 1;
arr[1] = 2;
arr[2] = 3;

list.addAll(arr);
for(var l, list)
   print(l + " ");      // 1 2 3
end;
//>