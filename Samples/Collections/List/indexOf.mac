import "..\\mac\\extra\\collections\\collections.mac";
var list = ArrayList();
//<
list.addItems(1, 2, 3, 4);
var result = list.indexOf(3);
println(result);            // 2
result = list.indexOf(6);
println(result);            // -1
//>