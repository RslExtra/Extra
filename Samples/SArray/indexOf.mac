import "../mac/extra/utils/SArray.mac";
//<
var arr = SArray(1, 2, 3, 4, 5, 2, null);

var ind = arr.indexOf(2);
println(ind);             // 1
ind = arr.indexOf(8);     // Поищем индекс неприсутсвующего элемента
println(ind);             // -1
ind = arr.indexOf(null);  // Также можно искать null
println(ind);             // 6
//>
