import "../mac/extra/utils/SArray.mac";
//<
import "../mac/extra/utils/Functions.mac";     // Импортируем генераторы
var arr = SArray(1, 2, 3, 4, 5);
var result = arr.filter(Predicates.GE(3));
result.toScreen();    // [3, 4, 5]
//>
