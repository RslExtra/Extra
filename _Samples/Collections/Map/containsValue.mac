import "..\\mac\\extra\\collections\\collections.mac";
var map = LinkedHashMap();

//<
map.putItems("key1", "val1", "key2", "val2", "key3", "val3");
var result = map.containsValue("val1");
println(result);        // True
result = map.containsValue("someValue");
println(result);        // False
//>